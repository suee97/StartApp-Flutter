import 'package:flutter/material.dart';

class StampEventJoin extends StatelessWidget {
  const StampEventJoin({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
