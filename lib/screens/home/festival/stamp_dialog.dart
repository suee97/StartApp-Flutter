import 'package:flutter/material.dart';

class StampDialog extends StatelessWidget {
  const StampDialog({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
