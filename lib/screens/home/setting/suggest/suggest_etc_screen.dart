import 'package:flutter/material.dart';

class SuggestEtcScreen extends StatelessWidget {
  const SuggestEtcScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text("SuggestEtcScreen"),),
    );
  }
}
