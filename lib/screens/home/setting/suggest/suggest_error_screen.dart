import 'package:flutter/material.dart';

class SuggestErrorScreen extends StatelessWidget {
  const SuggestErrorScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text("SuggestErrorScreen"),),
    );
  }
}
