import 'package:flutter/material.dart';

// 현재 버전, 관리자, 관리자이메일 등

class DevInfoScreen extends StatelessWidget {
  const DevInfoScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text("DevInfoScreen"),),
    );
  }
}
