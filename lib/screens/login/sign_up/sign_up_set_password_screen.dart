import 'package:flutter/material.dart';

class SignUpSetPasswordScreen extends StatelessWidget {
  const SignUpSetPasswordScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text("sign up set password screen"),),
      body: Center(
          child: Text("SignUp Set Password Screen")
      ),
    );
  }
}
