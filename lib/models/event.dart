import 'package:flutter/material.dart';

class Event {
  String title = "";
  String imgSrc = "";
  Color color = Colors.white;
  String link = "";
  String content = "";

  Event(this.title, this.imgSrc, this.color, this.link, this.content);
}